# Get ranges of the bounding box margins

Get ranges of the bounding box margins

## Usage

``` r
.getRanges(expand = 1.03, ranges = par3d("bbox"))
```

## Arguments

- expand:

  Expand margins.

- ranges:

  The bounding box.

## Value

A list with ranges.
