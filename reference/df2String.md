# Convert each row to a string.

Convert each row to a string.

## Usage

``` r
df2String(df, round = 2)
```

## Arguments

- df:

  Data frame.

- round:

  How many digits to round

## Value

A vector of strings.
